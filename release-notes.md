# Penraft 0.3.3

## 修复

- **数据统计不准已修复**：之前的版本如果发版时漏注入后端地址，埋点会打到本机 localhost 丢失。新版本即使忘了注入也会默认走生产 API，并对掉数据的设备自动补充注册，统计更可靠。

## 改进

- 后台 dashboard 新增「下载点击」面板，按位置（nav/hero/footer）和平台架构（macOS arm64/x86_64）分别看转化。
- 官网下载按钮埋点字段统一，便于聚合。
