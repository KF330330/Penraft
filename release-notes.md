# Penraft 0.4.1

## 🐛 修复：右键「Open With Penraft」不把窗口拉到前台

在 Finder 右键 `.md` 文件 → **Open With → Penraft** 时，如果 Penraft 已经在后台运行，文件会正确打开，但窗口本身不会切到前台，需要手动点 Dock 才能看到。

这一版修好了：

- 被最小化的窗口会自动恢复
- 被 `Cmd+H` 隐藏的窗口会重新显示
- 冷启动场景下 drain 完批量文件后也会兜底拉到前台

后台 / 最小化 / `Cmd+H` 隐藏 / 冷启动四种场景都已端到端验证通过。
