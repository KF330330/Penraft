# Penraft 0.4.6

🛡️ **标签页状态更安全**：修复在 macOS 文件访问权限异常时,重启后已打开的标签页会全部静默丢失的问题。现在启动读取标签状态失败会明确提示,且不会用空状态覆盖之前保存的标签列表。

🔔 **保存标签状态失败提示优化**:不再每次操作都重复弹出提示,只提示一次;若是权限导致的失败,提示中会附上解决指引(系统设置 → 隐私与安全性 → 文件与文件夹 → Penraft)。
